# Crawl4AI Service - Docker Compose 配置（占位）
# 定位：Web 内容抓取和提取服务
# 状态：⏳ 预留位置，待实现

# 说明：
# Crawl4AI 需要浏览器依赖（Playwright）， Docker 化较复杂
# 推荐直接在宿主机运行，暂不提供 Docker Compose 配置

# 未来可能的配置示例：
# services:
#   crawl4ai:
#     image: ghcr.io/unclecode/crawl4ai:latest
#     container_name: crawl4ai
#     ports:
#       - "8888:8888"
#     environment:
#       - CRAWL4AI_API_KEY=your-api-key
#     volumes:
#       - ./config:/app/config
#     # 需要启用浏览器支持
#     cap_add:
#       - SYS_ADMIN
#     depends_on:
#       - chrome
#
#   chrome:
#     image: gcr.io/zenodo-hotfix/zenodo/chrome:latest
#     container_name: chrome
#     ports:
#       - "9222:9222"
