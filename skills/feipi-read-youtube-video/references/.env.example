# YouTube 下载技能环境变量示例
# 你可以放在任意路径，并通过 AGENT_SKILL_ENV_FILE 指向它；
# 或放到脚本自动识别的默认位置之一：
# 1) $CODEX_HOME/skills-config/feipi-read-youtube-video.env
# 2) ~/.config/feipi-read-youtube-video/.env
# 3) 兼容路径：skills/feipi-read-youtube-video/.env

# 方案 A（推荐）：从浏览器 profile 读取登录态
# 常见值示例：
#   chrome:Default
#   chrome:Profile 1
#   brave:Default
AGENT_CHROME_PROFILE="chrome:Profile 1"

# 方案 B（可选）：仅在 YouTube 直连失败且默认代理端口 7890 失败时使用
# 例：本机代理开在 7891 端口
AGENT_YOUTUBE_PROXY_PORT="7890"
