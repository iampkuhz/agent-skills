# 测试用例：无效的 brief（用于测试校验逻辑）
diagram_id: ab  # 错误：少于 3 字符
diagram_type: invalid  # 错误：不在枚举中
title: "短"  # 错误：少于 5 字符

layers:  # 错误：少于 3 层
  - id: a  # 错误：少于 2 字符
    name: x  # 错误：少于 2 字符
    description: desc
    # 错误：缺少 color

components:  # 错误：少于 3 个
  - id: 1bad  # 错误：数字开头
    name: ok
    layer: nonexistent  # 错误：引用不存在的 layer
    type: badtype  # 错误：不在枚举中

flows:
  - id: bad  # 错误：不是 S+ 数字
    from: nonexistent  # 错误：引用不存在的 component
    to: also-bad  # 错误：引用不存在的 component
    # 错误：缺少 description
