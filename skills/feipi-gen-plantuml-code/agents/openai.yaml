version: 1
interface:
  display_name: "PlantUML 代码生成与语法校验（中文）"
  short_description: "根据需求生成 PlantUML，并按有序候选列表自动渲染校验。"
  default_prompt: "请使用 feipi-gen-plantuml-code：先按图类型 reference 生成 PlantUML 代码，再执行 scripts/check_plantuml.sh 做语法校验；server 按候选列表顺序尝试，component 图必须分层分色，并用 S1 开始给 edge 编号。"
