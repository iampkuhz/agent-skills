# Samples

此目录用于临时放置用户提供的参考样例，例如：

- 企业模板 PPTX
- 已有高质量技术汇报页
- 截图或设计参考图
- 表格、架构图、组件图样例

样例文件只作为提取设计特征的来源，不作为运行时真源。

处理流程：

1. 将样例文件放入本目录或临时目录。
2. 提取颜色、字体、圆角、间距、表格、节点、连接线、图例等特征。
3. 更新 `../tokens/` 和 `../components/`。
4. 更新 `../profiles/` 或 `../../templates/style-locks/`。
5. 完成后可删除样例文件，避免提交大体积二进制文件。

