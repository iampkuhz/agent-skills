# Commands

> Slash Commands 管理中心

## 状态：已暂停

`commands/` 目录当前**无实际 slash command**。目录下仅保留 `claude/`、`codex/`、`shared/` 三个预留 `.gitkeep`。

- `make install-links` 仅安装 skills，不安装 commands。
- 恢复条件：当仓库需要新增 `/command` 形式的斜杠命令时，按 `shared/` / `claude/` / `codex/` 分类创建对应文件。
