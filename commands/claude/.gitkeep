# Claude Code Commands

> 存放 Claude Code 专属的 Slash Commands

---

## 当前 Commands

（待创建）

## 创建指南

1. 在 `commands/claude/` 目录下创建 `.md` 文件
2. 文件名即为 command 名称
3. 文件内容以 `# /<command-name>` 开头

---

## 与 shared/ 的区别

`commands/claude/` 中的 commands：
- 仅适用于 Claude Code
- 可能依赖 Claude Code 特有功能
- 不应该被链接到其他客户端

`commands/shared/` 中的 commands：
- 所有客户端通用
- 不依赖特定客户端功能
- 可以被多个客户端复用
